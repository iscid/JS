// Sólo devuelve un resultado, debido a que las buenas prácticas indican que los ids son únicos.

const formulario = document.getElementById("formulario");
console.log(formulario); // devuelve todo el formulario

const card = document.getElementById("card");
console.log(card); // devuelve null ya que no existe ese id
